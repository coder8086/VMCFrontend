import { VideoCallContainer } from './video-call-container';

describe('VideoCallContainer', () => {
  it('should create an instance', () => {
    expect(new VideoCallContainer()).toBeTruthy();
  });
});
